{
  "name": "TIL-Joogumi",
  "version": "1.0.0",
  "description": "TIL blog by vuepress",
  "main": "index.js",
  "author": "Jooeun",
  "license": "MIT",
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "^1.9.7",
    "vuepress": "^1.9.7"
  },
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "git build && git push origin main && git subtree push --prefix=docs/.vuepress/dist origin gh-pages"
  }
}
